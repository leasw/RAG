.env
logs/*
!logs/turns/
!logs/turns/**
docs/
__pycache__/
*.py[cod]
.pytest_cache/

# 재생성 가능한 대용량 생성물 (100MB 초과, doc_rag.build_index로 재생성)
cache/
index/doc_rag/embeddings.npy

# 문서 코퍼스 전체 텍스트를 그대로 복제한 1회성 점검용 덤프 (원본은 index/doc_rag/chunks.sqlite3에 이미 있음)
.scratch/parsed_chunks.json
.scratch/parsed_chunks.jsonl
.scratch/parsed_chunks_shards/

# 이 저장소는 벡터RAG(doc_rag)만 공유한다 — 나머지 모듈은 로컬 작업용으로만 남기고 추적하지 않는다
graph_rag/
org_agent_mvp/
chat_server/
memory_seed/
viewer/
data/
index/graph_rag/
logs/
.claude/
.scratch/
